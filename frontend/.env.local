# Set to "true" to clear the onboarding localStorage key on every page load.
# This forces the welcome tour to replay, simulating a brand-new user sign-in.
VITE_FORCE_ONBOARDING=true
VITE_GOOGLE_API_KEY=AIzaSyCAPXMG4toGprLTdC2awosLldJ1SGBpeL8  # same key as Vercel / GCP Credentials → API key
VITE_GOOGLE_APP_ID=1043035653650   # GCP → Project settings → Project number
